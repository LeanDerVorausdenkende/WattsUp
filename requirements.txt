# ── Core data ────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
openpyxl>=3.1          # read .xlsx files
python-dotenv>=1.0     # load .env config in fetch scripts

# ── HTTP / API ────────────────────────────────────────────────────────────────
requests>=2.31

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7
plotly>=5.18           # interactive charts (dashboard / UI group)

# ── Notebooks ─────────────────────────────────────────────────────────────────
jupyter>=1.0
ipykernel>=6.0

# ── ML models ─────────────────────────────────────────────────────────────────
scikit-learn>=1.3      # metrics, walk-forward splits, linear baseline
lightgbm>=4.0          # main model
shap>=0.44             # explainability (SHAP values for LightGBM)
prophet>=1.1           # statistical benchmark

# ── Optional: deep learning bonus ─────────────────────────────────────────────
# torch>=2.0           # uncomment if attempting LSTM
# tensorflow>=2.13     # alternative to torch
